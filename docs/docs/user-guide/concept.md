The goal of EasyDynamics is to make it easy to fit QENS and powder INS data.

The fit is carried out by an `Analysis` object, which contains an `Experiment` that contains the data, a `SampleModel` that 
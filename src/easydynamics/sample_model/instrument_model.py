# instrument_model will contain resolution_model and background_model as well as offset? That seems reasonable.


# Will resolution_model know about convolution? Or will that be handled at a higher level?
# Probably higher level, which then takes sample_model and instrument_model
